spring.application.name=todo-management
# === JWT CONFIG ===
jwt.secret=MySuperSecretKey12345678901234567890
# 1 day in milliseconds
jwt.expiration=86400000                      

# === SERVER CONFIG ===
server.port=8081


# === DATABASE CONFIG ===
spring.datasource.url=jdbc:mysql://localhost:3306/Todo-ManagementDB
spring.datasource.username=root
spring.datasource.password=akash@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === JPA CONFIG ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# === CORS CONFIG (Optional if frontend is separate) ===
#spring.web.cors.allowed-origins=http://localhost:5173
#spring.web.cors.allowed-methods=*

# === LOGGING (Optional) ===
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

